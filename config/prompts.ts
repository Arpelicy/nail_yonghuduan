import type { AI_TASK_CONFIG } from "./ai";

// ─────────────────────────────────────────────────────────────────────────────
// 系统消息（双图任务通用）
// ─────────────────────────────────────────────────────────────────────────────
export const SYSTEM_TRYON = `\
你是一位专业 AI 美甲师，专注于超写实美甲试戴。

图片身份约定（每次任务均适用）：
- 第一张图（款式参考图 style_reference）：始终是美甲款式 / 参考来源。
- 第二张图（用户手图 user_hand）：始终是用户的手部照片，是需要被编辑的目标。

核心规则：
- 绝对不能重绘或重新生成用户手部结构、皮肤、手指或背景。
- 输出结果必须看起来像真实照片，而非数字叠加或拼贴。
- 本系统定位：超写实 AI 美甲试戴。`;

// ─────────────────────────────────────────────────────────────────────────────
// Step 1：标注指甲框
// gpt-5.4 · 输入不限 · 输出 1024×1024 · low · opaque
// ─────────────────────────────────────────────────────────────────────────────
export const PROMPT_BOX = `\
请给参考图中每个指甲用红色虚线框单独标出，要求：
- 每个框只包含一个指甲，框与框之间不得重叠或覆盖其他指甲区域。
- 框要尽量贴合指甲边缘，不要框得过大。`;

export const CONFIG_BOX: AI_TASK_CONFIG = {
  model:      "gpt-5.4",
  action:     "generate",
  size:       "1024x1024",
  quality:    "low",
  background: "opaque",
  inputFixed: false,
};

// ─────────────────────────────────────────────────────────────────────────────
// Step 2：提取透明背景正视图款式
// gpt-5.5 · 输入不限 · 输出 1024×1024 · medium · transparent
// ─────────────────────────────────────────────────────────────────────────────
export const PROMPT_EXTRACT = `\
请只读取输入图中红色虚线框内的指甲区域。

任务：将框选的指甲款式重新生成为一张干净的展示图，背景完全透明。

严格要求：
1. 生成全新展示图，不保留原始手部照片构图。
2. 只生成框内的指甲款式，不生成手指、皮肤、衣物或原始背景。
3. 各指甲分开排列，从左到右顺序对齐，大小和间距视觉上保持一致。
4. 尽可能忠实保留每个框内指甲的主色调、图案、纹理、装饰和整体风格，内部图案位置也尽量贴近原图。
5. 采用正面产品展示视角，适合美甲款式目录使用。
6. 背景必须完全透明。
7. 不添加文字、水印、边框、标签或额外装饰。
8. 保持原始从左到右的框选顺序。`;

export const CONFIG_EXTRACT: AI_TASK_CONFIG = {
  model:      "gpt-5.5",
  action:     "generate",
  size:       "1024x1024",
  quality:    "medium",
  background: "transparent",
  inputFixed: false,
};

// ─────────────────────────────────────────────────────────────────────────────
// 试戴基础配置（所有试戴任务共用）
// gpt-5.5 · 输入固定 1024×1536 · 输出 1024×1536 · medium · opaque
// ─────────────────────────────────────────────────────────────────────────────
const BASE_TRYON: AI_TASK_CONFIG = {
  model:      "gpt-5.5",
  action:     "edit",
  size:       "1024x1536",
  quality:    "medium",
  background: "opaque",
  inputFixed: true,   // 前端裁图已强制输出 1024×1536
};

// ─────────────────────────────────────────────────────────────────────────────
// 提示词 A：甲面迁移（保持甲型不变）
// 用户勾选"保持自身甲型不变"时使用
// ─────────────────────────────────────────────────────────────────────────────
export const PROMPT_TRANSFER = `\
任务：甲面图案迁移——将第一张图红色虚线框内的美甲图案迁移到第二张图对应指甲上。

第一张图（款式参考图）：一张手部照片，红色虚线框标注了需要迁移的美甲图案位置，请仔细识别每个框内的颜色、纹理、图案和装饰细节。
第二张图（用户手图）：用户的手，是编辑基础。

操作要求：
1. 以第二张图（用户手图）为基础，绝对不能改变手指形状、指甲长度、指甲弧度、肤色、背景或光线。
2. 只替换第二张图各指甲表面的视觉效果，将第一张图红色框内对应的美甲图案贴合到用户指甲上。
3. 按照从左到右的顺序严格一一对应——第一张图最左边的红框对应第二张图最左边的指甲，依此类推。
4. 迁移后的美甲图案必须看起来像真实涂在用户指甲上，贴合指甲的真实透视、弧度和质感，不能像贴纸效果。
5. 尽可能忠实保留第一张图中的颜色、图案、纹理、边缘细节和装饰。
6. 最终结果必须看起来像真实拍摄的手部照片——超写实美甲试戴效果。`;

export const CONFIG_TRANSFER: AI_TASK_CONFIG = { ...BASE_TRYON };

// ─────────────────────────────────────────────────────────────────────────────
// 提示词 B1：完整款式试戴（允许适度调整甲型）
// 用户未勾选"保持自身甲型不变"时使用
// 图1：美甲款式图（手戴美甲照片 / 产品图）  图2：用户手图
// ─────────────────────────────────────────────────────────────────────────────
export const PROMPT_FULL_TRYON = `\
任务：完整美甲款式试戴——将第一张图的美甲款式应用到第二张图用户手上。

第一张图（款式参考图）：美甲款式展示图，包含要复现的设计、颜色、图案和甲型。
第二张图（用户手图）：用户的手，是试戴目标。

操作要求：
1. 以第二张图（用户手图）为基础构图，绝对不能改变手指结构、肤色、背景或光线。
2. 将第一张图的美甲款式按从左到右顺序严格一一对应地应用到第二张图的对应指甲上。
3. 允许适度调整第二张图指甲的甲型大小和形状，以更好地贴合第一张图的款式比例和风格——这有助于提升视觉真实感和款式还原度。
4. 尽可能忠实复现第一张图的美甲效果，包括颜色、图案、纹理、装饰和边缘细节。
5. 最终结果必须看起来像真实拍摄的美甲试戴照片——超写实效果，不能是拼贴或数字叠加感。
6. 不添加多余的手指、指甲、配饰、文字或水印。`;

export const CONFIG_FULL_TRYON: AI_TASK_CONFIG = { ...BASE_TRYON };

// ─────────────────────────────────────────────────────────────────────────────
// 提示词 B2：完整款式试戴（保持甲型不变）
// 用户勾选"保持自身甲型不变"且上传的是款式图（非带框标注图）时使用
// 图1：美甲款式图（手戴美甲照片 / 产品图）  图2：用户手图
// ─────────────────────────────────────────────────────────────────────────────
export const PROMPT_FULL_TRYON_KEEP = `\
任务：美甲款式上妆（保持甲型）——将第一张图的美甲颜色、图案和装饰效果原样贴合到第二张图用户指甲上，不改变任何甲型。

第一张图（款式参考图）：美甲款式展示图，包含要复现的颜色、图案、纹理和装饰细节。
第二张图（用户手图）：用户的手，是编辑目标。

操作要求：
1. 以第二张图（用户手图）为基础，绝对不能改变手指形状、指甲长度、指甲弧度、肤色、背景或光线。
2. 将第一张图的美甲款式按从左到右顺序严格一一对应地应用到第二张图的对应指甲上。
3. 严禁调整指甲的形状、长度或弧度——只替换甲面的视觉效果。
4. 迁移后的美甲效果必须贴合用户指甲的真实透视、弧度和质感，不能像贴纸叠加。
5. 尽可能忠实保留第一张图中的颜色、图案、纹理、边缘细节和装饰。
6. 最终结果必须看起来像真实拍摄的手部照片——超写实美甲试戴效果。`;

export const CONFIG_FULL_TRYON_KEEP: AI_TASK_CONFIG = { ...BASE_TRYON };

// ─────────────────────────────────────────────────────────────────────────────
// 提示词 C：长度框延长
// 图1：detected 带红框款式手图（红框标注期望甲长区域 + 款式参考）
// 图2：用户干净手图（编辑目标）
// ─────────────────────────────────────────────────────────────────────────────
export const PROMPT_LENGTH = `\
任务：美甲延长 + 款式上妆——按照第一张图红色虚线框所示范围延长第二张图用户指甲并应用款式。

第一张图（款式参考图）：一张手部照片，每根手指上有红色虚线框，框的范围同时代表期望的指甲长度和要迁移的美甲款式。
第二张图（用户手图）：用户的干净手图，是编辑目标。

操作要求：
1. 以第二张图（用户手图）为基础，绝对不能改变手指形状、肤色或背景。
2. 将第二张图每根手指的指甲延长至第一张图对应红色虚线框所示的大致长度和覆盖范围，甲尖形状自然流畅。
3. 将第一张图红色框内的美甲款式按从左到右顺序严格一一对应地应用到每根延长后的指甲上。
4. 延长后的指甲长度、弧度和整体比例应自然真实，看起来像用户真实蓄甲并涂妆后的效果。
5. 尽可能忠实保留第一张图框内的颜色、图案、纹理和装饰。
6. 最终结果必须看起来像真实拍摄的手部照片——超写实美甲延长试戴效果。`;

export const CONFIG_LENGTH: AI_TASK_CONFIG = { ...BASE_TRYON };
